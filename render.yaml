services:
  - type: web
    name: meanwhile
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        value: sqlite:///./meanwhile.db
      - key: MARKET_DATA_PROVIDER
        value: MOCK